import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: ["100.85.52.91", "car.winmoresight.com"],
};

export default nextConfig;
